import { WaveCommand } from 'wave-shell'

export default {
  description: "{{ description }}",
  run: async (options) => {
    const { print } = options;

    print.info('{{ name }} command!');
  }
} as WaveCommand