#!/usr/bin/env bun
import { Cli } from 'wave-shell'
import { join } from 'path'

const devMode = existsSync(`${__dirname}/../src`)

const projectRoot = devMode ? join(__dirname, '..') : join(__dirname, '..', 'dist')
const cli = new Cli('{{ name }}', projectRoot)
cli.run()