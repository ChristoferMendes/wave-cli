#!/usr/bin/env bun
import { Cli } from 'wave-shell'
import { join } from 'path'

const isDevMode = existsSync(join(__dirname, '..', 'src'))

const projectRoot = isDevMode ? join(__dirname, '..') : join(__dirname, '..', 'dist')

const cli = new Cli('{{ name }}', projectRoot)
cli.run()
