import type { WaveCommand } from 'wave-shell'

export default {
  description: "{{ description }}",
  run: async (options) => {
    const { print } = options;

    print.info('Welcome to Wave CLI');
  }
} as WaveCommand